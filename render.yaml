services:
  - type: web
    name: ai-reels-studio-ai
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    healthCheckTimeout: 300
    envVars:
      - key: GEMINI_API_KEY
        sync: false